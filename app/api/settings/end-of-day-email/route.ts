/**
 * @deprecated Use GET/POST /api/attendance/end-of-day-email (same handlers).
 * Kept for backwards compatibility with older clients.
 */
export { GET, POST, dynamic } from '../../attendance/end-of-day-email/route'
